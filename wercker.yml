box: wercker/default
build:
  steps:
      - validate-wercker-step
  after-steps:
      - script:
          code: | 
            export WERCKER_SLACK_NOTIFY_INCOMMING_WEBHOOK_URL=$SLACK_INCOMMING_WEBHOOK_URL
            sh ./run.sh

